import { colors } from '@unocss/preset-mini';

export const graphTheme = {
    colors: {
        bg: {
            base: colors.zinc['900'],
            fineGrid: colors.zinc['800'],
            wideGrid: colors.zinc['950'],
        },
    },
};
